
describe("RavageTextExtensionComposite", () => {

    describe("TextExtensionComposite", () => {
        it("Should initialize", async () => {

        });

        it("And should add extension node element to node", async () => {

        });

        it("And should add new extensions for text component from npm site",
            async () => {

            });

        it("And should interact with TextExtensionNode", async () => {

        });
    });
});